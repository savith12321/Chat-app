<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.socket.io/4.3.2/socket.io.min.js" integrity="sha384-KAZ4DtjNhLChOB/hxXuKqhMLYvx3b5MlT55xPEiNmREKRzeEm+RVPlTnAn0ajQNs" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Chat App</title>
</head>
<body>
    <div class="input-group mb-3">
        <input type="text" class="form-control" placeholder="type a Message" id='message_input'>
        <button class="btn btn-outline-secondary" type="button" id="button-addon2" onclick="">Send</button>
    </div>
    <script>
        const name = prompt("Please type a user name");

        if (name == undefined || name == null) {
            window.location = "/error?message=the user name cant be null";
        }

        const socket = io.connect("http://localhost:3000")

        socket.on("connection")

        const sendMessage = () =>{  
            const input_box = document.getElementById("message_input");
            const message_to_send = input_box.value;
            input_box.value = "";

            socket.emit("message", `${name}: ${message}`)
        }
    </script>
</body>
</html>